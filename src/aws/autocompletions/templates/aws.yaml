---
name: shortcuts-aws

title: shortcuts-aws v2 cli

subcommand: s3
  summary: s3 commands
  subcommand: ls
    summary: List files / folders in the shortcuts-aws file system
    parameters:
      - name: s3uri
        completion:
          command_string: |
             _aws_s3_ls.sh "${CURRENT_WORD}"